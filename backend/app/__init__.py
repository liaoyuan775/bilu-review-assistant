"""
Bilu 笔录审查助手 — 后端入口包。

职责边界：
- 提供 FastAPI HTTP 接口，供前端调用发起审查、查询结果、人工分流。
- 通过 Qwen 多模态模型对电诈询问笔录执行"三现四流"规则审查。
- 使用 SQLite 持久化审查任务与结果，支持断点续查。

核心约束：
- 所有规则结果必须可溯源至笔录原文，模型不得编造证据。
- 系统不替代执法判断、案件定性或证据效力判断。
- 人工复核为必经环节，模型输出仅为辅助参考。
"""
